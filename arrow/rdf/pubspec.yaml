name: arrow_rdf
description: Chart-RDF serializer + deterministic IRI minter for Arrow (ARP-4).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  arrow_options:
    path: ../options
  arrow_swe:
    path: ../swe
  arrow_core:
    path: ../core
  charts_dart:
    git:
      url: https://gitlab.com/ninthhouse/charts_dart.git
      ref: main
  crypto: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
