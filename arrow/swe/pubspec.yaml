name: arrow_swe
description: Swiss Ephemeris facade for Arrow.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  arrow_options:
    path: ../options
  swisseph_rs: ^0.2.8
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  lints: ^6.0.0
  test: ^1.25.0
