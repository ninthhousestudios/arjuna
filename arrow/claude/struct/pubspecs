Let me scaffold out the actual files. I'll start with the foundation:

---

## 1. Root `melos.yaml`

```yaml
name: arrow
packages:
  - packages/**

command:
  bootstrap:
    usePubspecOverrides: true
```

---

## 2. `packages/arrow_options/pubspec.yaml`

```yaml
name: arrow_options
description: Shared configuration and enums for the Arrow calculation engine.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  test: ^1.24.0
```

---

## 3. `packages/arrow_swe/pubspec.yaml`

```yaml
name: arrow_swe
description: Swiss Ephemeris bindings and EphSnapshot for the Arrow calculation engine.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  arrow_options:
    path: ../arrow_options
  sweph: ^2.8.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  test: ^1.24.0
```

---

## 4. `packages/arrow_core/pubspec.yaml`

```yaml
name: arrow_core
description: Pure Dart derivation layer — signs, nakshatras, vargas, dignities, domain model.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  arrow_options:
    path: ../arrow_options
  arrow_swe:
    path: ../arrow_swe
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  test: ^1.24.0
```

---

## 5. `packages/arrow_calc/pubspec.yaml`

```yaml
name: arrow_calc
description: Pure Dart analysis layer — dashas, yogas, shadbala, ashtakavarga, jaimini.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  arrow_options:
    path: ../arrow_options
  arrow_swe:
    path: ../arrow_swe
  arrow_core:
    path: ../arrow_core
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  test: ^1.24.0
```

