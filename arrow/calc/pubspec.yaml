name: arrow_calc
description: Calculations layer for Arrow — avasthas, aspects, synodic state.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  arrow_options:
    path: ../options
  arrow_swe:
    path: ../swe
  arrow_core:
    path: ../core
  logging: ^1.3.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
