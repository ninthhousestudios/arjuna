# Handoff — Arrow Refactor

## Branch: `drona`

## Completed

- **Wave A** (all parts): SWE boundary sealed
  - A1, A2 (prior session): nakAyanamsa moved to SweConfig
  - A3: `includeStarData` → `calcAll` parameter
  - A4.1: `Body.sweId` → `swe/lib/src/body_swe_id.dart`
  - A4.2: `Star.sweName` → `swe/lib/src/star_swe_name.dart`
  - A4.3: Barrel no longer re-exports `SwissEph` or `dhruva.dart`
- **Wave C**: Body hierarchy flattened
  - `Graha` class deleted, `Karaka extends Planet` directly
  - `Sun` class deleted (unused on main path)
  - Varga simplified to two maps (`_planetMap`, `_karakaMap`)

## Next Up: Wave D — Extract varga math from Longitude

The file is `core/lib/src/longitude.dart` (681 lines). The full file has been read and understood. The task:

1. Create `core/lib/src/varga_math.dart` with top-level functions for each of the 14 private varga methods (_hora, _drekkana, _chaturthamsha, etc.)
2. Each function takes `(double lon, int adityaOffset)` plus any method-specific params, returns `VargaResult`
3. Extract the helper functions (`_eclipticSignNum`, `_eclipticInSign`, `_baseLon`, `_isOdd`) as private free functions in varga_math.dart
4. `Longitude._computeVarga` delegates to the new functions, passing `_adityaOffset`
5. Create `core/test/varga_math_test.dart` with edge-case tests (boundary crossings, sign-12 wrap, parivritti with various amsha)
6. One commit. Mechanical move — don't refactor the math.

The `_parivrittiDeity` helper and `_deityVargaByAmsha` const also move since they're only used by parivritti.

## Wave B (paused)

Still deferred per plan. Open questions in `docs/arrow-refactor-plan.md` need decisions before starting.

## Notes

- Generated files (.freezed.dart, .g.dart) are gitignored — run `dart run build_runner build` in each package after touching freezed classes
- `graha.dart` and `sun.dart` still exist as empty files (git rm denied by policy) — delete them manually or leave as-is
