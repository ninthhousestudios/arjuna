# Handoff — Arrow Refactor

## Branch: `drona`

## Last commit: `04cc717` — Fix double-subtraction bug (nak-frame pre-computation)

Replaced `nakAyanamsaValue` scalar with per-body/star pre-computed nak-frame longitudes on `EphSnapshot`. Six new flat maps. `SweFacade.calcAll` computes them directly via SWE native sidereal methods — no subtraction. All 830 tests pass.

## Pick up next

### 1. Enforce dhruva + nakEquatorial constraint (medium priority)

Dhruva is equatorial-only. `nakEquatorial=false` with dhruva produces meaningless results (ecliptic map stores the equatorial value as fallback). Add a validation assert in `ArrowOptions` or `SweConfig`/`CalcConfig` boundary that dhruva requires `nakEquatorial=true`.

### 2. Add sidereal nakshatra integration test (recommended)

The unit tests verify the plumbing, but there's no integration test that runs `calcAll` with a Lahiri sidereal config and checks that a body at sidereal 0° gets nakshatra 1 (Ashvini). The reviewer flagged this gap — the fix should be verified end-to-end.

### 3. Cusp nakshatra correctness (low priority)

`Cusp` constructs `Longitude` without `nakLongitude` (falls back to ecliptic longitude). For tropical-sign + sidereal-nak configs, cusp nakshatras are wrong. Options: compute nak longitudes for cusps in `calcAll`, or document the limitation.

## Completed waves

- **Wave A** (all parts): SWE boundary sealed
- **Wave C**: Body hierarchy flattened
- **Wave D**: Varga math extracted from Longitude
- **Wave B.1**: CalcConfig stripped from EphSnapshot
- **Wave B.2**: StarPosition introduced, 6 star maps collapsed to 2
- **Nak-frame fix** (`04cc717`): Double-subtraction bug fixed, sidereal positions computed natively

## Notes

- Generated files (.freezed.dart, .g.dart) are gitignored — run `dart run build_runner build` in swe/ after touching freezed classes
- `ArrowOptions` still exists in `options/` — it's the user-facing config bundle, just no longer on the snapshot
- SWE global state ordering in `calcAll` is critical: all sign-frame work must complete before nak-frame phase switches `setSidMode`
