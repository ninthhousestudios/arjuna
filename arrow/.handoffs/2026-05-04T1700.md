# Handoff — Arrow Refactor

## Branch: `drona`

## Completed

- **Wave A** (all parts): SWE boundary sealed
- **Wave C**: Body hierarchy flattened
- **Wave D** (`be0361f`): Varga math extracted from Longitude
  - 16 varga methods → `core/lib/src/varga_math.dart` as pure top-level functions
  - Longitude: 691 → 150 lines
  - Edge-case tests in `core/test/varga_math_test.dart`
- **Wave B.1** (`00f006d`): CalcConfig stripped from EphSnapshot
  - `EphSnapshot.options: ArrowOptions` → `EphSnapshot.sweConfig: SweConfig`
  - `SweFacade.calcAll` now takes `SweConfig` directly
  - 11 files changed (1 production, 10 tests)

## Next Up: Wave B.2 — Introduce StarPosition

Collapse 6 star maps on EphSnapshot into 2:

| Current (6 maps) | Target (2 maps) |
|---|---|
| `starsEcliptic: Map<Star, BodyPosition>` | `stars: Map<Star, StarPosition>` |
| `starsEquatorial: Map<Star, BodyPosition>` | |
| `starData: Map<Star, StarData>` | |
| `customStarsEcliptic: Map<String, BodyPosition>` | `customStars: Map<String, StarPosition>` |
| `customStarsEquatorial: Map<String, BodyPosition>` | |
| `customStarData: Map<String, StarData>` | |

Where `StarPosition` holds ecliptic `BodyPosition` + equatorial `BodyPosition` + optional `StarData`.

### Touch points (from research)

1. `swe/lib/src/star_position.dart` — new freezed class
2. `swe/lib/src/eph_snapshot.dart` — 6 fields → 2
3. `swe/lib/src/json_converters.dart` — 4 star converters → 2 (minimal, not optimized)
4. `swe/lib/src/swe_facade.dart` — build `StarPosition` objects, simplify locals
5. `core/lib/src/fixed_star.dart` — 3 map lookups per factory → 1
6. `core/lib/src/chart.dart` — `.starsEcliptic.keys` → `.stars.keys`
7. `calc/lib/src/zodiac/build_ecliptic13.dart` — `snap.starsEcliptic` → `snap.stars` + `.ecliptic`
8. Tests: `swe/test/swe_facade_star_test.dart`, `core/test/fixed_star_test.dart`, `swe/test/ecliptic13_integration_test.dart`

### Decisions already made

- Two maps (enum-keyed + string-keyed), not one unified map
- JSON converters: defer optimization (B.3) until JSON for stars is actually needed — just make them compile
- Coordinate as `arrow_swe` version bump for downstream consumers

## Wave B.3 — Deferred

Collapse/optimize JSON converters for star maps. Not needed until star JSON is used in production.

## Notes

- Generated files (.freezed.dart, .g.dart) are gitignored — run `dart run build_runner build` in swe/ after touching freezed classes
- `ArrowOptions` still exists in `options/` package and `Presets` — it's the user-facing config bundle. Just no longer on the snapshot.
