# Handoff — Arrow Refactor Wave A in Progress

**Branch:** `drona` at `a009d22`
**Plan:** `docs/arrow-refactor-plan.md` — full sequencing for A3, A4, B, C, D.
**Diagnosis:** `docs/arrow-refactor.md` — original review (unchanged).

## What's done

- **A1** (`931ce1b`) — `nakAyanamsa` moved from `CalcConfig` to `SweConfig`.
- **A2** (`a009d22`) — Sidereal nakshatra ayanamsa now wired through to
  `Longitude.nakshatra` and `Longitude.pada`. Fixes refactor candidate
  #6 (silent correctness bug). All 4 packages green: 286 core, 67 swe,
  393 calc, 55 options.

## Pick up next

Resume Wave A from **A3**. Order from the plan:

1. **A3** — Move `includeStarData` out of `SweConfig` into a `calcAll`
   parameter. Trivial — see plan §A3 for the steps.
2. **A4** — Seal SWE wire-format leaks: `Body.sweId` and `Star.sweName`
   to private maps in `swe/`, stop barrel-exporting `SwissEph` and
   `dhruvaGcEquatorial`. Three sub-commits (A4.1/A4.2/A4.3).
3. **C** — Flatten body hierarchy. Independent; can interleave.
4. **D** — Extract varga math from `Longitude`. Independent; can interleave.

Then **stop** and decide approach for **B** (EphSnapshot reshape) —
plan §B lists the open questions to settle first.

## Context for next session

- `dart run build_runner build --delete-conflicting-outputs` needs
  Bash permission *per package directory*. The permission granted for
  `options/` does not carry over to `swe/`. Ask Josh to run it manually
  or grant a broader permission.
- sutra-guard blocks `Edit` on load-bearing files (blast radius ≥ 10)
  until `sutra_impact` is called on the symbol. Plan ahead — pre-call
  `sutra_impact` for known load-bearing edits to avoid retry cycles.
- `nakAyanamsaValue` propagation pattern (snapshot → SkyObject getter
  override → Longitude param default) is repeatable for any future
  snapshot-sourced value flowing into Longitude.
- Custom non-dhruva ayanamsas (truesidereal, vedanga, equal28) currently
  log a warning and use 0 in `SweFacade._calcNakAyanamsa`. Implement
  when a user actually selects one.
- Pre-existing unstaged changes (`.gitignore`, `CLAUDE.md` in parent
  dir) are not from this refactor work.
