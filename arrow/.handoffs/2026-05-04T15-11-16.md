# Handoff — Arrow Refactor

## Branch: `drona`

## Completed

- **Wave A** (all parts): SWE boundary sealed
- **Wave C**: Body hierarchy flattened
- **Wave D** (`be0361f`): Varga math extracted from Longitude
- **Wave B.1** (`00f006d`): CalcConfig stripped from EphSnapshot
- **Wave B.2** (`fa3fcd1`): StarPosition introduced, 6 star maps collapsed to 2
  - New `StarPosition` freezed class: ecliptic + equatorial `BodyPosition` + optional `StarData`
  - `EphSnapshot` fields: `stars: Map<Star, StarPosition>`, `customStars: Map<String, StarPosition>`
  - 4 JSON converters replaced with 2 (`StarPositionMapConverter`, `StringStarPositionMapConverter`)
  - `SweFacade.calcAll` builds `StarPosition` inline (starData folded into star computation loop)
  - Consumers updated: `FixedStar` (1 lookup instead of 3), `Chart`, `buildEcliptic13`
  - 10 files changed, all 830 tests pass

## Next Up: Wave B.3 — Collapse JSON converters (deferred)

Deferred until star JSON is actually needed in production. The current 2 converters work but aren't optimized.

## All Waves Complete (except B.3)

The core refactor is done. Remaining work:
- B.3 (JSON converter optimization) — deferred, low priority
- Version bump to `arrow_swe` 0.2.0 for downstream consumers
- Update `docs/api-reference.md` to reflect new `StarPosition` type

## Notes

- Generated files (.freezed.dart, .g.dart) are gitignored — run `dart run build_runner build` in swe/ after touching freezed classes
- `ArrowOptions` still exists in `options/` package and `Presets` — it's the user-facing config bundle. Just no longer on the snapshot.
