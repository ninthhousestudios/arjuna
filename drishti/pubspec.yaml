name: drishti
description: MCP server for Arrow astrological calculations.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  quiver_embedded:
    path: ../quiver/embedded
  mcp_dart: ^2.1.0
  logging: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
