name: quiver_embedded
description: Vayu — local embedded Quiver for in-process Arrow access.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  arrow_options:
    path: ../../arrow/options
  arrow_swe:
    path: ../../arrow/swe
  arrow_core:
    path: ../../arrow/core
  arrow_calc:
    path: ../../arrow/calc
  quiver_core:
    path: ../core
  logging: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
