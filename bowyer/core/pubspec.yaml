name: bowyer_core
description: Pure Dart core logic for Bowyer admin panel.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  quiver_core:
    path: ../../quiver/core
  logging: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
